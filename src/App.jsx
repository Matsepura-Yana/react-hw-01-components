import { Description } from 'Description'

const App = ({ data }) => {
    return (
        <div className="profile">
            <Description data={data} />
        </div>
    )
}

export default App
